package com.anthooop.colision.core.di

import org.koin.core.module.Module
import org.koin.dsl.module

// Bindings for cross-cutting concerns (Logger, CrashReporter, Analytics,
// DispatcherProvider). Story 1.4 fills this in with concrete declarations.
val coreModule: Module = module {
}
